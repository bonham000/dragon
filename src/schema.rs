table! {
    users (id) {
        id -> Int4,
        email -> Varchar,
        username -> Varchar,
        uuid -> Varchar,
        push_token -> Varchar,
        experience_points -> Int8,
        score_history -> Varchar,
        settings -> Varchar,
    }
}
